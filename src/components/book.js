import styled from "styled-components";
import select from "../utils/select.js";
export default styled.article`
  border-bottom: 1px solid ${select("theme.border")};
`;

export const Footer = styled.footer``;
